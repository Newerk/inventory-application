extends layout 

block content 

    div(class="header-solid")
        h1= title

    if partsArr.length    
        ul(class="ul-container") 
            each part in partsArr
                li(class="content-link") 
                    a(href=part.url) #{part.name} 
        
