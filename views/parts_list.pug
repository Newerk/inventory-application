extends layout 

block content 

    div(class="header-solid")
        h1= title

    if partsArr.length    
        ul(class="ul-container") 
            - for(var i = 0; i < partsArr.length; i++) {
                if i % 2 === 0
                    li(class="content-link bg-dark") 
                        a(href=partsArr[i].url) #{partsArr[i].name} 
                else    
                    li(class="content-link bg-light") 
                        a(href=partsArr[i].url) #{partsArr[i].name}
            - }
        

