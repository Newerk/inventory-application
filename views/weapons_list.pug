//depending on the middleware used, it will display a list of all weapons, or only back weapons, or only arm weapons (will use a switch case)
extends layout 

block content 
    h1= title

    if filteredWeapons.length 
        ul
            li 
                a(href=filteredWeapons.url) #{filteredWeapons.name} 
        